Revision history for api-dashboard

0.06 2026-04-30
    - renamed the saved ajax workers to local names under `dashboards/ajax/`
    - aligned the extracted page with DD's `/ajax/api-dashboard/...` skill-prefixed ajax route contract
    - kept the stronger route-contract and worker-body verification

0.05 2026-04-29
    - restored compatibility with the current DD 3.24 flat ajax route contract after proving `/ajax/<skill>/...` is not active in the shipped runtime
    - kept the stronger route-contract and worker-body verification

0.04 2026-04-29
    - rechecked the DD 3.24 ajax router and proved it still resolves the flat `api-dashboard-*` ajax file contract
    - kept the extracted page and ajax worker filenames aligned with that current DD contract
    - replaced the exact-copy regression with route-contract and worker-body verification

0.03 2026-04-29
    - restored the missing `api-dashboard` ajax worker files under `dashboards/ajax/` so the extracted skill carries its request handlers with the page
    - added Docker verification that the ajax files match the DD source page worker bodies exactly

0.02 2026-04-29
    - added Docker-generated Playwright screenshots for the API dashboard README
    - used a temporary public API demo during screenshot generation without storing that API config in the repo
    - added verification for README screenshot assets

0.01 2026-04-29
    - created the api-dashboard skill by extracting the current DD API dashboard page into a skill-local dashboard asset
    - added Docker tests for exact-copy integrity, bookmark structure, and browser layout smoke coverage
    - added skill-local README, docs, and ticket records
